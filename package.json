{
  "name": "wa-unknown",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -f public/service-worker.js && next build && next export && netlify deploy --dir out",
    "deploy-prod": "rm -f public/service-worker.js && next build && next export && netlify deploy --prod --dir out"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.1.1",
    "fastify-compress": "^3.2.2",
    "next": "^9.4.4",
    "next-offline": "^5.0.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-intl-tel-input": "^8.0.4"
  }
}
